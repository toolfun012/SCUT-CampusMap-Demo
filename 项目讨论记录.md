# 华工校园导览小程序项目讨论记录

记录时间：2026-07-28

## 1. 项目背景

原项目是一个数据结构课程设计：虚拟路网导航系统。

已有能力包括：

- 虚拟路网生成
- 点、边、道路数据结构
- Dijkstra 最短路径算法
- 附近点查询
- 拥堵/路况模拟
- 路线高亮和地图交互

HR 给出的建议大意是：

> 不要只停留在课程设计的基础功能上，要跳出课设限制，融入自己的需求。已有产品形态本身没有竞争力，基于已有形态叠加自己的需求，才更像有竞争力的产品。

因此，项目方向从“通用导航系统”调整为：

> 面向华工新生和访客的校园导览小程序。

项目重点不再是和高德、百度地图竞争，而是解决一个具体场景：

> 新生第一次来到华工大学城校区，不知道当前位置、目的地点、热门打卡点和推荐游览路线。

## 2. 项目定位

推荐项目名称：

> 华工新生校园导览小程序

一句话描述：

> 基于校园地图、图结构建模和最短路径算法，为华工大学城校区新生提供地点查询、路线规划、打卡推荐和主题游览路线的小程序。

简历表述方向：

> 基于原有虚拟路网导航课设，重新设计为面向华工新生的校园导览小程序。将校园道路、建筑入口和景点抽象为图结构节点，支持目的地搜索、多入口建筑最优入口选择、Dijkstra 路线规划和主题游览路线推荐，使项目从算法演示升级为贴近真实校园场景的产品原型。

## 3. 为什么不做普通导航

普通导航已经有高德、百度、腾讯地图等成熟产品。

如果项目只是：

- 显示地图
- 计算最短路径
- 标出路线

那么它很容易被认为是已有导航产品的低配版。

更有竞争力的方式是：

- 聚焦华工校园内部
- 面向新生导览
- 做校园内部建筑、入口、景点、打卡点
- 做主题路线，比如新生报到路线、拍照路线、食堂路线
- 保留数据结构算法核心，同时体现产品思考

## 4. 关于真实定位

微信小程序可以通过 `wx.getLocation` 获取手机定位：

```js
wx.getLocation({
  type: 'gcj02',
  success(res) {
    const lat = res.latitude
    const lng = res.longitude
  }
})
```

但是校园道路间隔较近，手机定位精度不一定够。

大致情况：

- 室外开阔区域：可能 5-20 米
- 楼宇密集、树多、靠近高楼：可能 20-50 米
- 室内：更不稳定

因此不能强依赖精准定位到某一条小路。

更稳的设计：

```text
微信定位
-> 找附近 3 个候选路口/建筑入口
-> 用户确认当前位置
-> 将确认节点作为路径规划起点
```

同时，为了保证 demo 不依赖定位权限，第一版应支持：

> 用户手动输入或选择当前位置和目标位置，直接计算路径。

## 5. 关于地图底图

之前讨论过是否可以扒取百度/高德地图数据。

结论：

> 不建议扒取、截图拼接、抓包或批量保存百度/高德地图数据。

原因：

- 百度、高德地图数据属于商业地图服务
- 官方协议通常限制抓取、缓存、离线保存、镜像、脱离服务使用地图数据
- 对简历项目来说，数据来源不干净会留下隐患

更推荐的选择：

### 方案 A：腾讯地图原生底图

适合后续微信小程序版本。

使用方式：

```text
微信小程序 map 组件
+ 腾讯地图底图
+ 自己的 markers 地点标注
+ 自己的 polyline 路线绘制
+ 自己的 Dijkstra 算法
```

优点：

- 与微信小程序适配最好
- 地图底图精度高
- 不需要自己维护完整底图
- 可以后续接腾讯位置服务

缺点：

- 地图视觉不是自定义校园导览图
- 需要在腾讯地图坐标体系下维护节点经纬度

### 方案 B：华工官网校园地图作为图片底图

适合第一版无定位权限 demo。

已查到华工官网大学城校区地图页面：

- 页面：https://www2.scut.edu.cn/scutnxq/8558/list.htm
- 图片：https://www2.scut.edu.cn/_upload/article/images/93/4f/cd5b74b84b9da6321eab063d23de/9f22b701-d479-4ca9-a8d5-24644d2673a8.jpg

对照百度地图截图和官网图，校区大体结构是对得上的：

- 校区整体轮廓一致
- 北门、南门、西门位置大体一致
- 生活区、教学区、湖区、体育场等相对位置大体一致
- 适合作为校园导览 demo 底图

但它是导览图，不是严格 GIS 测绘图。

所以第一版应避免直接用图片像素距离作为真实距离，而是在 `edges.json` 中手动维护道路距离。

## 6. 当前推荐技术路线

第一阶段做无权限 demo：

```text
官网校园地图图片
+ 手动标注地点和入口节点
+ 手动标注道路边
+ Dijkstra 最短路径
+ canvas 或覆盖层绘制路线
+ 用户手动选择起点和终点
```

第二阶段接入微信小程序和腾讯地图：

```text
腾讯 map 组件
+ 自己的校园 POI 数据
+ 自己的路网节点经纬度
+ Dijkstra 算法
+ polyline 绘制路线
+ 可选 wx.getLocation 定位吸附
```

## 7. Demo 第一版目标

第一版只做基础功能：

1. 显示华工大学城校区地图
2. 标注校内建筑、景点、校门、饭堂等地点
3. 用户选择当前位置和目标位置
4. 使用 Dijkstra 计算最短路径
5. 在地图上显示路径

暂时不做：

- 实时定位
- 路线跟随导航
- 打卡推荐
- 观赏路线推荐
- 后端服务
- 登录和收藏

但需要预留数据字段和接口，方便后续扩展。

## 8. 建筑多入口设计

对于几面都有道路的大建筑，应该设置多个入口节点。

例如图书馆有东门、西门、南门：

```text
library_east
library_west
library_south
```

地点本身作为 `place`：

```js
{
  id: 'library',
  name: '图书馆',
  entranceIds: ['library_east', 'library_west', 'library_south']
}
```

用户搜索“图书馆”时，不直接导航到建筑中心，而是：

```text
当前位置
-> 分别计算到图书馆各入口的最短路径
-> 选择距离最短的入口
-> 作为最终目的地
```

这样更符合真实校园导航。

## 9. 数据结构设计

### places.js

面向用户展示的地点。

```js
module.exports = [
  {
    id: 'library',
    name: '图书馆',
    type: 'study',
    tags: ['学习', '打卡'],
    hotScore: 90,
    sceneScore: 85,
    entranceIds: ['library_east', 'library_west'],
    description: '校园核心学习场所，适合新生熟悉学习环境。'
  },
  {
    id: 'north_gate',
    name: '北门',
    type: 'gate',
    tags: ['入口', '报到'],
    hotScore: 95,
    sceneScore: 70,
    entranceIds: ['north_gate_main'],
    description: '大学城校区主要出入口之一。'
  }
]
```

### nodes.js

算法节点，包括路口、建筑入口、校门、景点入口等。

如果使用图片底图：

```js
module.exports = [
  {
    id: 'north_gate_main',
    name: '北门入口',
    kind: 'entrance',
    x: 520,
    y: 75
  },
  {
    id: 'road_001',
    name: '北门内侧路口',
    kind: 'road',
    x: 510,
    y: 125
  },
  {
    id: 'library_east',
    name: '图书馆东入口',
    kind: 'entrance',
    x: 640,
    y: 340
  }
]
```

如果后续接定位，可扩展为：

```js
{
  id: 'north_gate_main',
  name: '北门入口',
  kind: 'entrance',
  x: 520,
  y: 75,
  latitude: 23.000001,
  longitude: 113.000001
}
```

### edges.js

道路连接关系。

```js
module.exports = [
  {
    from: 'north_gate_main',
    to: 'road_001',
    distance: 30,
    roadType: 'walkway',
    sceneScore: 2,
    crowdLevel: 1
  },
  {
    from: 'road_001',
    to: 'library_east',
    distance: 220,
    roadType: 'main',
    sceneScore: 4,
    crowdLevel: 2
  }
]
```

第一版 Dijkstra 使用：

```text
weight = distance
```

后续可以扩展：

```text
风景路线：weight = distance - sceneScore * reward
避开拥挤：weight = distance + crowdLevel * penalty
```

## 10. Dijkstra 规划逻辑

基本流程：

```text
选择起点地点
-> 获取起点入口或当前位置节点
-> 选择终点地点
-> 获取终点所有入口节点
-> 对每个终点入口跑 Dijkstra
-> 选择总代价最小的路径
-> 返回 path 节点序列
-> 根据节点 x/y 或 lat/lng 画线
```

建筑多入口的规划函数：

```js
function planToPlace(graph, startNodeId, place) {
  let best = null

  for (const entranceId of place.entranceIds) {
    const result = dijkstra(graph, startNodeId, entranceId)

    if (!best || result.distance < best.distance) {
      best = {
        entranceId,
        distance: result.distance,
        path: result.path
      }
    }
  }

  return best
}
```

## 11. 微信小程序基础目录建议

```text
miniprogram/
  app.json
  app.js
  pages/
    map/
      map.wxml
      map.wxss
      map.js
      map.json
  data/
    places.js
    nodes.js
    edges.js
  utils/
    graph.js
    geo.js
    route.js
```

如果使用官方校园图作为图片底图，页面结构更适合：

```text
scroll-view / movable-area
+ image 显示校园地图
+ canvas 绘制路径
+ view 显示地点标记
```

如果使用腾讯地图底图，页面结构更适合：

```text
map 组件
+ markers
+ polyline
```

## 12. 无定位权限版本

为了让 demo 不依赖用户授权，第一版必须支持手动选择起点和终点。

交互可以是：

```text
起点：搜索/选择当前位置
终点：搜索/选择目标地点
按钮：开始导航
结果：显示距离、预计时间、路线
```

示例：

```text
起点：北门
终点：图书馆
-> 自动选择图书馆最近入口
-> 显示路径
```

后续定位只是增强能力：

```text
定位当前位置
-> 吸附到最近节点
-> 自动填入起点
```

## 13. 后续可扩展功能

### 定位吸附

```text
wx.getLocation
-> 获取 latitude / longitude
-> 找最近 3 个节点
-> 用户确认
-> 作为起点
```

### 热门打卡推荐

每个地点增加：

```js
hotScore
sceneScore
freshmanScore
tags
visitMinutes
```

推荐逻辑：

```text
根据用户选择主题
-> 按分数筛选地点
-> 用 Dijkstra 串联路线
```

### 主题路线

可以预设：

- 新生报到路线
- 拍照打卡路线
- 食堂探索路线
- 半小时快速游览路线
- 一小时深度游览路线

数据结构：

```js
{
  id: 'freshman_route',
  name: '新生报到路线',
  placeIds: ['north_gate', 'admin_building', 'library', 'canteen', 'dorm_area']
}
```

### 路线偏好

后续可以做：

- 最短路线
- 风景路线
- 少拥挤路线
- 少转弯路线
- 优先主干道路线

## 14. 当前最小开发计划

第一天目标：

1. 建立小程序项目目录
2. 放入官网校园地图作为底图
3. 手动标注 5 个地点
4. 手动标注 10-20 个节点
5. 写 Dijkstra
6. 实现从北门到图书馆的路径显示

第二阶段目标：

1. 扩展到 20-30 个地点
2. 扩展到 60-100 个道路节点
3. 做起点/终点搜索
4. 支持多入口建筑
5. 显示距离和预计步行时间

第三阶段目标：

1. 接微信定位
2. 做定位候选点确认
3. 加热门打卡点
4. 加推荐路线
5. 优化界面和简历展示

## 15. 面试讲法

可以这样介绍项目改造逻辑：

> 原项目是数据结构课程设计，核心是虚拟路网生成和 Dijkstra 最短路径算法。后来我意识到它的使用方式还停留在输入节点编号的算法演示层面，离真实用户比较远。因此我将项目重新设计为面向华工新生的校园导览小程序，把校园建筑、道路和入口抽象为图结构节点，支持用户手动选择或定位当前位置，搜索目的地后自动选择最佳入口，并使用 Dijkstra 进行路径规划。后续还可以扩展热门打卡点推荐、主题游览路线和个性化路线偏好，使项目从课程作业升级为一个有明确用户场景的产品原型。

## 16. 当前关键决策

目前建议采用：

```text
第一版：官网校园图图片底图 + 手动节点 + Dijkstra + 无定位权限
后续版：微信小程序 map 组件/腾讯地图 + 经纬度节点 + 定位吸附
```

原因：

- 第一版实现最快
- 不依赖定位权限
- 数据完全可控
- 能体现算法能力
- 后续可以平滑扩展到真实定位和腾讯地图

